#include "main.h"
#include <stdio.h>

/**
 * swap_int: swapping two file
 * @a: first parameter
 * @b: second parameter
 * 
 * Return: always 0.
 */

void swap_int(int *a, int *b)
{
	
	int t
	int t = a;
	a = b;
	b = a;
}
